| Time step | O&M sample text |
|---|---|
| T1 | On Novaris ControlDrive-721 Plus, record the as-found shutdown behavior, the exact key-off sequence used by the operator, and whether the service warning mentions high voltage retained, because discharge verification is only meaningful when the controller state and command path are known. |
| T2 | Isolate the traction battery, remove the service cover over the capacitor compartment, and connect a CAT III rated meter across the DC-link capacitor studs while leaving the bleed resistor pack, precharge contactor branch, and discharge-monitor connector visible for simultaneous observation. |
| T3 | Re-energize only as required to establish a normal ready state, then command key-off and watch the DC bus decay continuously from operating voltage rather than taking a single late reading, because a stalled plateau near 60 to 100 V often points to a failed bleed path that would be missed by a delayed check. |
| T4 | Compare the measured decay curve with the service target, such as below 30 V within 5 s on this controller family, and note whether the drop is smooth, stepped, or interrupted by relay chatter, since the shape of the curve is as diagnostic as the endpoint. |
| T5 | If discharge is slow or incomplete, inspect the resistor pack leads, solder joints, discharge relay contacts, and the low-voltage enable signal to the bleed circuit, making sure the fault is not caused by a sleeping control board that never commands the resistor path closed. |
| T6 | Repeat the shutdown test after correcting any visible connection issue or after isolating the suspect resistor branch, and compare the new decay trace against the first trace so the record shows which exact hardware changed the behavior. |
| T7 | Before closure, verify the capacitor studs are back under the protected cover, confirm the discharge-monitor connector lock is fully home, and recheck that no measurement lead or temporary jumper remains inside the controller cavity. |
| T8 | Release the controller only after the final note identifies the verified discharge hardware as resistor pack, discharge relay, capacitor stud connection, or control-enable circuit, and includes the measured start voltage, end voltage, and elapsed decay time in plain English. |
