| Time step | O&M sample text |
|---|---|
| T1 | Log the trigger for the BMS communication dropout on Kestrel CellNet-482 AWD, including which CAN frames disappear first and whether the event follows vibration, wake transition, charger connection, temperature change, or one specific harness movement that can be repeated on demand. Note whether the first loss is the BMS heartbeat, cell-voltage data, or temperature broadcast so the dropout review begins from one real message boundary rather than from a broad network complaint. |
| T2 | Put the network to sleep, isolate the local supply, and expose the BMS communication connector, CAN twisted pair, shield bond, connector lock, intermediate clip points, and the branch path leading toward the nearest splice or gateway node. Keep the pair visible from connector rear to splice entry because twist loss, shield damage, or edge contact often sits between supports and not inside the connector cavity itself. |
| T3 | Inspect the connector cavity, CAN terminal fit, shield drain crimp, backshell shape, pair twist balance, and strain-relief area where the wires exit the housing, looking for pin spread, poor shield termination, conductor strain, or a distorted connector body that can open under movement. Compare the suspect branch with the nearest healthy twisted pair so a flattened pair, opened drain crimp, or widened terminal is tied to one local communication path. |
| T4 | Measure static bus resistance through the BMS branch with the network asleep, then compare that reading with the gateway-side value and the expected combined-termination range near 60 ohms so an open termination, partial short, or branch-isolation problem is localized before dynamic testing begins. Record the resistance both at the BMS connector and upstream of the nearest splice so the break can be assigned to one side of the branch if the values do not match. |
| T5 | Wake the network and backprobe CANH and CANL at the BMS connector while observing the same pair upstream, checking whether the dropout appears as a voltage collapse, open branch, unstable bias near the nominal 2.5-volt center, shield-related disturbance, asymmetrical waveform, or controller-side silence. The live comparison has to show whether the bad behavior is already present upstream or appears only at the BMS-side connector and twisted-pair segment. |
| T6 | Flex the harness at the clip points, shield transition, connector boot, and nearby bracket edge while logging communication status, because intermittent dropout usually follows one repeatable mechanical trigger instead of a random time pattern with no physical cause. Keep the movement small and repeatable so one exact clip point, boot exit, or bracket touch zone can be named as the reproduction trigger. |
| T7 | If the dropout can be reproduced, split the branch at the nearest splice or intermediate connector and repeat the observation so the fault is assigned cleanly to the BMS connector, twisted-pair segment, shield bond, splice, or controller communication input. This split must separate upstream network health from the local BMS branch before the controller itself is marked suspect. |
| T8 | Reconnect the protected interfaces, repeat the wake and communication cycle, and document the confirmed dropout boundary with the exact failing hardware named instead of leaving the diagnosis at DTC, frame-loss, or generic network level only. The boundary should end at one connector cavity, one twisted-pair span, one shield bond, or one controller input where the waveform difference was actually confirmed. |
| T9 | Hand the vehicle onward with the resistance result, reproduced trigger condition, live signal comparison, upstream-versus-downstream waveform behavior, and the named hardware fault path attached to the communication-dropout confirmation record. The handoff has to preserve enough detail that the next repair starts at the exact dropout segment instead of reopening the whole CAN branch. |
