| Time step | O&M sample text |
|---|---|
| T1 | On Aurex ThermLoop-659 AWD, record the complaint as wrong hose restored after service, branch misidentification during refill, or repeated thermal fault after manifold work, and identify which inlet or outlet branch is suspected before any hose is moved. |
| T2 | Make the thermal branch safe and expose the suspect hose joint, clamp, sensor connector, branch marker, and the first upstream and downstream support points without altering the current route or the natural hose twist. |
| T3 | Compare the branch marker, hose diameter, support path, and connected component so the inlet and outlet branches are identified by physical routing and not by label memory alone. |
| T4 | If the branch identity is uncertain, trace the hose from the manifold to the first downstream device and compare it with the parallel branch, noting clamp position, sensor location, and support order on both sides. |
| T5 | After any branch correction, restore the supports and confirm that the identified hose follows the correct path with no crossover, twist, or tension against the sensor connector, clamp line, and the nearest support clip. |
| T6 | Repeat the same thermal demand that originally exposed the complaint and verify that the restored branch now shows the expected temperature and flow behavior for its identified path. |
| T7 | Close the task only after the branch is named explicitly as the correct or incorrect inlet or outlet path, and the release condition records the final manifold-to-device route, clamp order, sensor relationship, repeat thermal behavior, and the corrected support-point sequence at the branch. |
