| Time step | O&M sample text |
|---|---|
| T1 | On Velorian ControlDrive-328 LR, record the inverter complaint as phase-current plausibility fault, reduced torque, or intermittent limp mode during high load, and capture which phase is affected together with motor temperature, DC current, and whether the fault appears only after vibration or thermal soak. |
| T2 | Safe the inverter and expose the LEM phase-current sensor module, connector X-ICS3, the secondary lock, the sensor harness clip, the low-voltage shield plate, and the nearby coolant elbow so the signal connector and local support can be inspected without changing the natural harness path. |
| T3 | Inspect connector X-ICS3 for partial latch engagement, terminal set-back, seal compression loss, bent secondary lock, and witness polish at the first clip point that would show the harness has been oscillating the connector instead of being supported by the clip bracket. |
| T4 | Measure supply, ground, and signal integrity at the connector while the harness is static and again while the branch is gently flexed at the backshell and the first clip point, because sensor plausibility faults often come from intermittent terminal motion rather than a failed current-sensor element. |
| T5 | Compare the affected phase connector state with the neighboring phase-sensor connector, looking for differences in drag feel, terminal height, CPA seating, or harness strain that can explain why only one phase channel reports unstable values. |
| T6 | If the connector checks good, extend the inspection to the sensor module mounting and shield plate, confirming the module is seated squarely and not being preloaded by the harness or the shield edge into a false sensor fault. |
| T7 | Reconnect and secure the branch in the correct clip order, then verify that the connector body, seal line, and first harness bend remain stable through a repeat power-up and load simulation. |
| T8 | Finish the diagnosis only after the fault is named as X-ICS3 terminal retention, secondary-lock issue, harness-strain input, sensor-module seating, true phase-current sensor failure, or no defect reproduced on the inspected phase channel, and after the repeat power-up or load simulation confirms stable signal behavior on the previously affected phase with the connector, clip, and shield plate in final service position. |
