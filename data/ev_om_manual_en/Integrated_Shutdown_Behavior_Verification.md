| Time step | O&M sample text |
|---|---|
| T1 | On Arcline SystemSync-272 EV, define the complaint as battery drain after parking, contactors staying awake after key-off, or multiple modules refusing to enter sleep after BMS or junction-box service. Record battery voltage, current DTC set, and the shutdown condition that reproduces the issue, because the later sleep judgment must be compared with the same key-off path and the same repaired hardware state. |
| T2 | Expose the BMS wake-line splice WL32, contactor feedback connector CJB-X2 at the HV junction box, and gateway connector C230 at the left dash side so those three signals can be observed from upstream request to downstream feedback. Keep the splice, connector locks, and first harness clips visible instead of burying them under trim during the check. |
| T3 | Command the vehicle from ready state to full key-off and monitor wake-line voltage, contactor feedback voltage, gateway sleep status, and overall low-voltage draw through the complete shutdown timer. A healthy sequence should let the wake line fall toward 0 V, the feedback input drop out, and vehicle current settle to the normal parked band after the defined sleep interval. |
| T4 | If one signal stays active, inspect WL32, CJB-X2, and C230 for partial latch, backed-out terminal, moisture trace, or harness load from the clip-to-bracket path. Compare the upstream splice and the downstream connector faces before separating anything, because a false-active contactor feedback and a held-high wake line can create the same parking-drain complaint while belonging to different hardware boundaries. |
| T5 | Isolate the suspect branch one point at a time by separating the wake-line splice or the junction-box feedback connector, then repeat the key-off sequence under the same conditions. When the sleep current recovers only after one branch is opened, keep the fault on that hardware path instead of blaming every module that merely stayed awake in response to the held signal. |
| T6 | Name the fault boundary explicitly as false-active contactor feedback at CJB-X2, wake-line hold at WL32, connector or terminal condition at C230, or a healthy wiring path with the keep-awake source inside one control module. Release is not allowed until the boundary is assigned to one interface or ruled out with measured shutdown timing and retained-voltage evidence. |
| T7 | Reassemble the trim only after two complete shutdown cycles confirm normal sleep entry, normal parked current, and no renewed held-high signal at the checked branch. The accepted closeout is a stable key-off transition with the repaired wake path restored and the final sleep boundary documented in hardware terms rather than as a generic battery-drain complaint. |
