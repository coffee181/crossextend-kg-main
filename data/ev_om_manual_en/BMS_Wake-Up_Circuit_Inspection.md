| Time step | O&M sample text |
|---|---|
| T1 | Define the BMS wake-up fault on Trion Control-433 LR by noting whether the symptom is no wake, delayed wake, false repeated wake, or wake without a stable online state, and record low-voltage level, sleep current, last wake source, and any charger or door-handle trigger present before the controller area is opened. Keep the intake state tied to one real wake request so the later voltage and timing checks are judged against the same source path that originally failed. |
| T2 | Put the network to sleep, isolate the local low-voltage feed, and open the controller area so the wake-up pin, wake-line connector, low-power fuse, header pin, ground return, splice section, and nearby clip points can be traced from source to controller input without breaking the path into guesses. Maintain visibility from the upstream wake source to the BMS header because a false wake diagnosis often starts when the branch is reduced to disconnected fragments too early. |
| T3 | Inspect the wake-line connector housing, terminal seating, fuse holder, ground eyelet, splice section, and harness clip points for corrosion, heat tint, loose fit, bent pins, poor crimp support, or strain that could interrupt the wake command or hold the line in the wrong state. Any green oxide at the fuse blades, eyelet witness loss at the ground stud, or rear-seal pullback at the connector should be marked now because each one changes the fault boundary differently. |
| T4 | Backprobe the wake-up input and the upstream wake source, then cycle the command while comparing voltage, timing, and drop across the fuse, connector, splice, and header pin rather than relying on a single reading taken only at the controller end. Record both the command rise and the release-to-sleep recovery so a delayed edge, weak pull-down, or drop under load is tied to one segment of the wake chain instead of to the controller generally. |
| T5 | Determine whether the wake signal changes normally while the controller fails to respond, or whether the fault is upstream and the wake command never reaches the BMS input with the correct amplitude, timing, pull-down behavior, or recovery after the command is removed. This split has to place the problem either on the controller input boundary or on the upstream fuse, connector, splice, or source path before any part is blamed. |
| T6 | Check the local ground path and low-power supply feed in the same pass, because an unstable ground eyelet, poor fuse-seat condition, or voltage drop across the low-power branch can imitate a wake-line fault at the controller pin even when the wake source is healthy. Compare the low-power feed during sleep, rising wake command, and stabilized online state so transient supply collapse is not confused with a missing wake signal. |
| T7 | Reassemble the inspected branch, repeat sleep and wake behavior through two full cycles, and close the inspection only after the final note ties the reproduced wake event to one named hardware boundary at wake-line fuse, wake connector terminal, splice section, ground return, or BMS wake input pin level. The accepted record is a wake diagnosis that states both the trigger source and the exact point where the wake command was lost, delayed, or confirmed normal. |
