| Time step | O&M sample text |
|---|---|
| T1 | Begin the output-terminal overheat review on Helion E-Box-367 Plus by recording which terminal showed heat evidence, whether the trigger was a no-drive complaint, thermal event, or visual discoloration found during another repair, and what the as-found state of the terminal cover and surrounding insulation looked like. Capture whether the discoloration sits on the cable side, bus side, or cover side of the joint so the overheat boundary starts from one real interface. |
| T2 | Isolate the high-voltage system, confirm safe discharge, and open the output-terminal area so the cable lug, terminal stud, nut, washer stack, bus-bar face, insulating boot, and local cable support bracket are visible without changing the natural cable angle. Keep the cable angle and boot clocking readable because side load is often part of the heat history at the terminal stack. |
| T3 | Inspect the overheat pattern at the nut flats, lug face, stud thread, washer stack, bus-bar contact surface, and boot interior, distinguishing local contact heating from external heat exposure by comparing color change, oxidation, and compression witness on each layer. One-sided oxide, blue heat tint at the stud shoulder, or a clean shadow under only part of the lug should be tied to the exact layer where clamp load was lost. |
| T4 | Remove the terminal hardware in sequence and lay the stack out by order, then inspect the underside of the cable lug and the bus-bar landing face for pitting, fret marks, oxide buildup, or contact-shadow patterns that indicate reduced clamp load or poor seating. Keep each washer and nut indexed by position so a wrong stack order or collapsed washer can later be separated from damage in the lug or bus face itself. |
| T5 | Measure resistance across the terminal joint and compare the result with the corresponding healthy output terminal, making sure the probes land on clean metal surfaces and not on discolored thread or washer edges that would distort the reading. If a loaded drop check is available, compare the suspect side and healthy side under the same current path so the electrical loss is anchored to one terminal stack instead of to a general output-circuit complaint. |
| T6 | Check the cable routing and support condition near the output stud, because a side-loaded cable lug, missing strain-relief support, or twisted cable stack can relax clamp load and create localized heating even when the nut remains present. Read the route from cable clamp to stud so the mechanical boundary is placed on one unsupported span or one rotated lug rather than on the terminal generally. |
| T7 | Compare the suspect terminal stack with the healthy phase-side or neighboring output terminal after the inspection, checking whether washer height, cable angle, torque witness, and contact imprint differ in a way that confirms the hot joint belongs to one hardware layer rather than to general enclosure heating. The comparison should make the failed stack geometry explicit before any part is replaced. |
| T8 | Decide whether the overheat source belongs to the cable lug, stud thread, bus-bar face, washer order, or cable-support condition, and record the failure at that exact hardware layer instead of using only a generic terminal overheat note. The fault boundary has to end at one named landing face, thread, washer stack, or support path backed by both heat evidence and resistance comparison. |
| T9 | Restore the protective boot and cover only after the work order captures terminal-stack order, measured resistance, visible heat damage, cable-support condition, and the exact part or interface requiring replacement or rework. The release note should let the next repair start directly at the failed terminal layer without reopening the area to rediscover where the hot joint sat. |
