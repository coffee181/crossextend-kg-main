| Time step | O&M sample text |
|---|---|
| T1 | Inside Helion ControlCab-605, trace the disturbed harness group by naming the exact branches that lost labels or clamp support, such as the X-axis servo power lead, encoder twisted pair, 24 V I/O bundle, or spindle-fan supply. Record which terminal blocks, drive plugs, and duct exits are involved before moving the first cable tie. |
| T2 | Open the cabinet to full service position and expose the affected harness path from upper finger duct DUCT-U1 down to lower EMC clamp rail EMC-R1, keeping neighboring drive heat sink, fan shroud, and relay bank visible. Harness restoration is not only a labeling task if the bundle has shifted into an airflow path or started pulling on a terminal group. |
| T3 | Match each conductor and ferrule to the electrical print and the physical destination, then mark every missing or ambiguous wire with the correct machine naming format before new labels are installed. A mislabeled encoder pair and an unlabeled 24 V sensor branch may both look like traceability issues, but they create very different fault risk during later service. |
| T4 | Inspect ferrule crimp quality, shield termination, drain-wire landing, clamp pressure, finger-duct exit radius, and cable slack between the source connector and the first fixed support point. If a bundle sags into the drive-fan inlet or pulls sideways on a plug shell, keep the boundary on support geometry rather than on the label alone. |
| T5 | Measure live 24 V level at source terminal XT24-1 and at remote load XT24-R3 while gently moving the restored bundle through its final routed position, then compare the drop with the pre-adjustment reading. This step separates a simple labeling job from a hidden loose terminal or cable-span issue that only appears under harness movement. |
| T6 | Install the correct sleeves or markers, refit DIN-rail clamps and edge supports, and route the bundle so power, feedback, and low-level signal branches remain separated where the cabinet design requires it. Confirm that the shield clamp seats against bare metal, that the fan inlet stays open, and that no small pair is crushed under a large bundle clip. |
| T7 | Power the cabinet and run the affected functions while inspecting the relabeled harness path for terminal heating, connector walk-out, fan interference, and vibration at the clamp points. The corrected state has to survive both initial energization and steady cabinet airflow, not just a static cabinet-open inspection. |
| T8 | Complete the cabinet restoration only when the note names exact harness groups, labels restored, clamp points corrected, measured live values, and final routed condition from duct exit DUCT-U1 to terminal destination XT24-R3 or drive plug. The release boundary is a harness path that is readable, mechanically supported, electrically stable under live check, and clear of fan, heat sink, and sharp panel edges. |
