| Time step | O&M sample text |
|---|---|
| T1 | On Arcline ToolChange-673, record the complaint as pot switch alarm at station 6, intermittent confirmation loss during index, or a switch that fails only under real carousel motion. Capture the as-found station number, the exact alarm, and whether the switch passes in manual actuation. |
| T2 | Expose the pot switch, trip flag, switch bracket, slot hardware, M12 connector, and the cable strain relief while preserving the original bracket position. Keep the trip flag and switch nose visible together so trigger geometry can be judged directly instead of inferred from the control screen. |
| T3 | Manually trigger the switch and then slow-index the carousel into station 6 while measuring electrical state, trigger point, and flag-to-switch overlap. If the switch works by hand but misses during motion at the same flag position, keep the boundary on bracket rigidity, flag alignment, or cable disturbance rather than on the switch contact alone. |
| T4 | Inspect the bracket slot, flag straightness, connector retention, and cable support from upper bracket face to lower strain-relief clip. A flexible bracket or a cable branch that pulls the switch body sideways can create the same alarm as a weak switch core, so both mechanical and electrical evidence are required before replacement. |
| T5 | Measure the final trigger window after the suspected hardware is corrected, comparing station 6 with one adjacent station so the switch is not adjusted into a narrow one-station-only condition. This comparison keeps the release boundary on real carousel geometry instead of a lucky trigger point that only passes in one position. |
| T6 | Correct only the identified switch, flag, bracket, or cable issue, then repeat station checks at station 6 and at neighboring stations with the same approach speed. Confirm that the flag enters the trigger window squarely and that the cable no longer biases the switch body at the end of travel. |
| T7 | Reinspect the switch nose, bracket screws, connector latch, and strain-relief clip after the repeated cycles because the first successful indexes can still shift a weak bracket or a tight cable branch. This second inspection proves that the corrected geometry survives real carousel motion rather than a single setup pass. |
| T8 | Close the work only after the report names the exact failed boundary, such as switch body, trip flag, bracket drift, or connector terminal, and ties it to repeatable station confirmation and final trigger-window measurement. Accept the repair when station 6 and the comparison stations all trigger reliably under the same index conditions that previously produced the alarm. |
