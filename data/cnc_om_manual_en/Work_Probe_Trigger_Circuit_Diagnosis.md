| Time step | O&M sample text |
|---|---|
| T1 | On Aurex VMC-620, record the work-probe complaint as missed trigger, double trigger, or no signal, and note whether it occurs on one stylus direction or across all probe touches. Capture the active probing macro, spindle orientation, and the exact feature location where the failure first appeared. |
| T2 | Expose probe body OMP-40, stylus assembly, kinematic seat, battery or interface module, cable or optical receiver path, and control-side input connector XPR-1 without disturbing the original setup geometry. Keep the probe body and the immediate signal path visible together so a small mechanical shift can be separated from an input-channel fault. |
| T3 | Trigger the probe manually in X+, X-, Y+, and Z directions while watching control input DI-PROBE and recording trigger repeatability, then compare those results with a static continuity or input-state check. If one direction requires noticeably more overtravel before the signal changes, keep the boundary on the stylus or kinematic seat rather than on the control input alone. |
| T4 | Inspect stylus tightness, probe-body seating, kinematic faces, cable strain relief, and connector drag from the stylus tip to the first fixed support point. A loose stylus and a weak connector can both create intermittent probe behavior, but they leave different evidence in trigger repeatability and loaded cable response. |
| T5 | Reproduce the probing event at the original work offset and compare trigger behavior with the bench check while the machine remains in the same probing geometry. This split between static trigger and in-position trigger keeps the repair on one real probe boundary instead of attributing every failure to macro logic. |
| T6 | Correct only the identified stylus, seat, cable, receiver, or input boundary, then restore the branch through its original support path so the final setup matches production geometry. Confirm that the stylus is torqued correctly and that the signal branch is not preloaded by a cable bend at the receiver or cabinet connector. |
| T7 | Repeat the original probing macro on the same feature and on one comparison feature, then compare trigger timing and repeatability with the pre-repair condition. The machine cannot be released on a single manual trigger if the same coordinate move still causes missed or double hits. |
| T8 | Close the work only after the report names the exact failed boundary, such as stylus assembly, kinematic seat, receiver path, cable span, or control input channel DI-PROBE, and ties it to repeated trigger behavior under the original probing condition. Accept the repair when the probe triggers repeatably in all required directions and survives the restored machine configuration. |
