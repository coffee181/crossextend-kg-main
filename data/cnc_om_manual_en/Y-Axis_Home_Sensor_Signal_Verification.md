| Time step | O&M sample text |
|---|---|
| T1 | On Velorian VMC-690, record the Y-axis home complaint as missing signal, drifting home position, or noisy trigger at reference return, and save the current home offset before any change is made. Note the approach direction and the machine coordinate where the fault appears so the later correction can be compared with the original trigger behavior. |
| T2 | Expose home sensor PS-YH1, slotted bracket, trip dog DG-YH1, cable branch, and connector XHY-1 while keeping the axis at a safe point that still preserves the as-found sensor-to-dog relationship. Keep sensor body and dog face visible together because bracket drift and dog drift create different correction paths. |
| T3 | Measure sensor gap at PS-YH1 and record the coordinate at which the signal changes as dog DG-YH1 approaches from the normal home direction, then compare that coordinate with the saved offset and the reverse-direction approach if permitted. A shift in trigger coordinate with stable electrical signal keeps the boundary on bracket or dog geometry rather than on the control input path. |
| T4 | Inspect bracket slot witness, dog fastener seating, cable strain, and connector latch while the area remains undisturbed. If the cable branch is pulling the bracket rearward or the dog face is rounded at one edge, the trigger point can drift even though the sensor itself still switches cleanly. |
| T5 | Correct only the identified bracket, dog, or cable support issue, then repeat the trigger test from both approach directions with the same feed and decel settings. Confirm that the sensor gap is within target and that the bracket sits against its mounting land without rocking when the cable settles. |
| T6 | Command full home return cycles until the final coordinate is repeatable, then inspect bracket screws, dog witness, and cable clamp again for movement. The machine cannot be released on one successful home if the hardware shifts after repeated decel loads. |
| T7 | Close the work only after the report names the exact boundary, such as sensor-bracket drift at PS-YH1, loose trip dog DG-YH1, cable pull, connector issue at XHY-1, or unstable sensor body, and ties it to measured trigger coordinates. Accept the repair when Y-axis home return remains repeatable from both directions with the restored sensor-to-dog geometry. |
