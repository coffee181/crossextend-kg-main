| Time step | O&M sample text |
|---|---|
| T1 | On Velorian VMC-600, record the Y-axis complaint as missed reference return, overtravel during homing, or non-repeatable zero, and note the exact homing sequence that fails. Capture the saved offset, approach speed, and machine coordinate of the first bad return so later proof uses the same reference logic. |
| T2 | Verify that the axis can move safely, then expose home sensor PS-YH2, trip dog DG-YH2, encoder marker path ENC-MK1, limit chain input, and nearby cable support while keeping the axis near the reference area. Keep the mechanical home hardware and feedback hardware visible together because the same failed return can belong to either boundary. |
| T3 | Run a controlled reference return and record sensor change at PS-YH2, encoder marker capture on ENC-MK1, decel point, and final zero position in one sequence. Compare the mechanical trigger point with control response so a late decel or missed marker is not misread as a drifting dog. |
| T4 | Inspect the home sensor bracket, dog fasteners, encoder marker integrity, connector latch, and cable support in the order they influence the reference sequence from mechanical trigger to electrical capture. If the mechanical trigger is stable but zero still moves, keep the next split on marker capture or control response instead of on the dog. |
| T5 | Correct only the failing boundary, then repeat reference return several times from the same starting coordinate and from a second starting coordinate if the control allows it. This confirms whether the repair fixed the sequence itself rather than accidentally hiding the problem under one approach condition. |
| T6 | Measure final zero repeatability after the corrected returns and compare it with the machine target and with the pre-repair spread. A successful home cycle without repeatable final zero is not enough to close a reference-return complaint. |
| T7 | Reinspect dog, bracket, cable support, and marker-related hardware after the repeated cycles because homing decel can shift weak brackets or strained cables even after the first successful pass. This extra check prevents a marginal reference fix from leaving the bay as accepted. |
| T8 | Run one final fully closed reference cycle with all guards and cable supports in production position, then compare the result with the open-service measurements. The real release boundary is the fully assembled homing sequence, not the convenient open test. |
| T9 | Close the work only after the report names the exact reference-return boundary, such as dog position DG-YH2, sensor bracket PS-YH2, marker capture path ENC-MK1, connector issue, or control-side decel response, and ties it to measured repeatability. Accept the repair when Y-axis zero returns consistently to target under the same sequence that originally failed. |
