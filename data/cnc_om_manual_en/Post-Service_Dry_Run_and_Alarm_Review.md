| Time step | O&M sample text |
|---|---|
| T1 | Once service work is complete on Velorian MillCenter-870, define the dry-run objective by naming the repaired subsystems, the alarm or motion symptom that must not return, and the exact program segment that previously exposed it. Note the required zero returns, tool stations, air pressure, and guarded state before the first dry cycle starts. |
| T2 | Restore the machine to full service configuration and expose only observation points that remain visible with guards installed, such as spindle head drain DR-SP1, toolchanger window, door-status lamps, coolant return, and repaired cable or hose supports. The dry run has to evaluate the real machine configuration rather than an open-panel convenience setup. |
| T3 | Execute the chosen dry-run program through the same axis reversals, spindle orient, toolchange, door transition, and coolant commands that previously triggered the complaint, and record the exact block or event where each subsystem changes state. Keep the sequence representative enough that a marginal interlock, valve, or support clip is challenged under realistic timing. |
| T4 | Watch servo load, spindle load, air-pressure recovery at PG-A1, and timing of door, changer, and orient signals at moments the repaired hardware is active, then compare those values with the previous failure condition if it was recorded. If the event remains clean electrically but a hose or harness moves toward an unsafe support point, keep the boundary on routing rather than on the controller. |
| T5 | Stop at the first completed cycle and inspect the serviced areas for connector walk-out, fresh leak, cover rub, loose bracket, delayed valve release, or contact witness that only appears after real motion. A dry run is valuable only if the physical machine is reread after the sequence, not just during it. |
| T6 | Run the same sequence a second time without intervention and compare the repeated servo load, signal timing, and physical witness marks with the first pass, because intermittent faults often disappear once and then return on the immediate next cycle. This second pass also verifies that the repaired path survives a hot restart condition. |
| T7 | Recheck the most critical repaired boundary, such as hose-to-bracket path, connector-to-clip path, valve-to-manifold joint, or arm-to-pocket clearance at station ST-14, after the second run with the machine still in guarded condition. The accepted state must be stable in the same mounted geometry that production uses. |
| T8 | End the dry-run review only when the note states the exact program scope, the operating values observed, the hardware boundaries watched, and the final repeat-cycle result without renewed alarm or witness change. The machine can advance to cutting only when both dry cycles finish cleanly and the serviced components remain secure, dry, and correctly routed at their final support points. |
