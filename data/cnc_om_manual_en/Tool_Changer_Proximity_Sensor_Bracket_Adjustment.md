| Time step | O&M sample text |
|---|---|
| T1 | On Helion ToolChange-520, record the complaint as proximity sensor early trigger, missed station confirmation, or physical target contact, and identify the exact sensor bracket before adjustment. Note whether the fault appears during forward index, reverse index, or arm approach to the pocket. |
| T2 | Expose the proximity sensor body, slotted bracket, target flag, cable branch, and the adjacent mounting plate while holding the changer at the service position that still shows the as-found gap. Keep the sensor and target flag visible together so trigger distance can be tied to real machine geometry. |
| T3 | Measure switching distance and trigger repeatability through the intended motion path rather than by a static feeler gap alone, then compare the result with the service target. If the sensor switches early only when the bracket flexes near one end of slot travel, keep the boundary on bracket geometry or slot damage instead of on the sensor face. |
| T4 | Inspect the bracket for bend, slot wear, screw seating, and cable pull from the upper support to the sensor body. Compare the target path with a neighboring healthy station if the design allows it, because small differences in bracket angle or target height often decide whether the trigger window is valid. |
| T5 | Adjust the bracket, restore correct cable support, and recheck trigger repeatability through several indexed positions and one full tool-change approach. The repaired sensor must hold its gap and switching window once the cable settles into the final motion path, not only while the mechanism is parked. |
| T6 | Measure the final trigger distance and confirm that no physical contact occurs between sensor face and target during the full approach path. This keeps the release boundary on a safe switching window instead of a marginal setting that only happens to clear one station. |
| T7 | Run repeated index and approach cycles, then inspect bracket screws, slot witness, cable clip, and target alignment again because repeated changer motion often reveals a bracket that drifts back to the old position. The machine cannot be released if the target window degrades after the first successful cycle. |
| T8 | Record the adjusted bracket position, final trigger distance, cycle range used for proof, and any cable-support correction in one traceable note. The closeout should allow the next technician to reproduce the same sensor setup without reopening the history. |
| T9 | Close the work only after the report names the exact failed boundary, such as bent bracket, worn slot, cable pull, target misalignment, or failed sensor body, and ties it to repeatable switching under motion. Accept the repair when the sensor triggers consistently with no contact and no renewed miss across the tested changer positions. |
