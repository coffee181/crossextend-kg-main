| Time step | O&M sample text |
|---|---|
| T1 | On Velorian VMC-610, record the X-axis home complaint as overtravel at reference return, missed home, or home repeatability outside target, and save the current home offset before any bracket movement. Note the machine coordinate, the approach direction that fails, and the original proximity-switch gap so the later correction can be compared with a real baseline. |
| T2 | Expose X-axis home proximity switch PS-XH1, slotted bracket, trip dog DG-XH1, cable branch, bracket mounting screws, and nearby way-cover section while keeping axis at a safe position that still shows original sensor-to-dog relationship. Preserve witness paint on bracket and dog because a small drift can be more informative than the final adjusted gap. |
| T3 | Jog the axis slowly into the dog from the home side and record the exact machine coordinate at which the switch changes state, then repeat from the opposite direction if allowed by the control. Compare that trigger point with the stored offset and with the required switch gap to determine whether the fault belongs to bracket shift, dog movement, or inconsistent switch response. |
| T4 | Measure sensor gap at PS-XH1 and inspect dog face DG-XH1, bracket slot, screw seating, and cable strain from upper bracket to lower clamp. A correct electrical sensor can still miss home if dog edge is rounded or bracket rotates under fast approach, so geometry must be read with trigger data. |
| T5 | Loosen the bracket only enough to reset the switch location, then tighten it and repeat the trigger test in both directions. If the change in trigger point follows bracket movement cleanly, keep the fault boundary on bracket position or screw retention rather than on the sensor body or the control home logic. |
| T6 | Inspect the trip dog for edge damage or slip on its mount and verify that the cable branch is not pulling the switch rearward as the way cover settles. This extra check prevents a correct gap from drifting out of range again once the cover and cable return to their normal supported positions. |
| T7 | Command full home return from both directions and compare repeatability, decel distance, and final machine coordinate with the pre-repair values. A good result requires repeatable switching and repeatable final reference position, not only a single successful home cycle from one approach direction. |
| T8 | Recheck bracket screws, dog witness marks, and cable clamp after the repeated home cycles because several reference faults return only after the bracket sees real decel load. Confirm that the way-cover section clears the switch and does not contact the cable or bracket at home. |
| T9 | Document the corrected switch gap, final trigger coordinate, and the exact hardware boundary that was found, such as bracket drift, loose dog, cable pull, or failed sensor repeatability. The release condition is a stable home sequence from both directions with the proximity switch and trip dog holding their corrected relationship. |
| T10 | Return the axis to normal production condition only after the work record links saved offset, adjusted bracket position, trigger coordinates, and final repeatability in one traceable closeout. Accept the repair when home boundary at PS-XH1 and DG-XH1 is named precisely and reference return remains stable through repeated cycles with covers and clamps restored. |
