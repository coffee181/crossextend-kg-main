| Time step | O&M sample text |
|---|---|
| T1 | On Trion SafetyLoop-640, begin the HVIL closed-loop check by naming every component recently opened or restored, such as service disconnect, lid switch, header connector, or interlock jumper section, and save the original faulted state. The loop proof has to start from the actual serviced path. |
| T2 | Verify static continuity through the full loop first and then through each sub-section if needed, so any open or high-resistance point is localized before functional tests begin. Record the value at the named sub-sections rather than only at the full loop. |
| T3 | Command the relevant wake or ready sequence and confirm that the system recognizes a closed interlock without intermittent drop during connector touch, cover loading, or mild harness movement. The functional state has to match the real operating condition that previously failed. |
| T4 | Inspect the recently serviced connector locks, switch plungers, and harness supports at the same time because a loop that passes at rest can still fail when the hardware settles into its closed position. A closed-loop proof without the physical reread is incomplete. |
| T5 | Repeat the sequence after full closure of covers and service access panels so the accepted state reflects the real operating configuration and the final preload on the loop branches. This is where temporary recovery usually disappears if support geometry is still wrong. |
| T6 | Compare the final loop behavior with the neighboring reference or the original baseline only after the same wake or ready state is reproduced, so the judgment remains grounded in one repeatable event. The comparison should clarify exactly which segment recovered. |
| T7 | Reinspect the named loop segments after the repeated proof because connector seating, switch plunger travel, or harness preload can change once the enclosure is fully closed. This post-proof check keeps the diagnosis on the actual loaded loop. |
| T8 | Finish the HVIL closed-loop verification only when the record names the verified path, the components specifically confirmed closed, the functional state used, and the final continuity or state result under full closure. The release boundary is an interlock loop that remains stable through repeated final-assembly checks with no renewed drop at the serviced segments. |
