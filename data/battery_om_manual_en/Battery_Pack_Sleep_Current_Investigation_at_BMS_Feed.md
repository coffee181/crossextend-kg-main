| Time step | O&M sample text |
|---|---|
| T1 | On Velorian SleepPack-612, record the measured sleep current, vehicle state, and elapsed time after shutdown before touching the BMS feed branch, because sleep-current diagnosis depends on exact timing. Save the current trace together with the shutdown sequence used. |
| T2 | Expose the BMS feed fuse, feed connector, wake line, ground return, and any attached low-voltage accessory branches while keeping the system otherwise in its natural post-shutdown state. The sleep-current path has to be read without waking the system accidentally. |
| T3 | Measure current draw at the pack and observe whether the BMS feed remains active, pulses, or decays as expected over time, then compare that behavior against the wake line and communication state. This shows whether the problem sits in feed retention, wake retention, or an attached branch load. |
| T4 | Isolate the BMS feed branch or sub-branches one at a time if current remains elevated, recording the drop associated with each removal so the excessive draw is tied to a named retained circuit rather than to the pack generally. The isolation order has to preserve the original timing context. |
| T5 | Inspect the branch that preserves the high draw for stuck wake command, internal controller wakefulness, shorted accessory path, or fuse and connector preload that keeps the feed alive after shutdown. A retained current path must land on one connector, fuse, or wake signal, not on a vague sleep complaint. |
| T6 | Recreate the shutdown sequence after correcting the identified wake or feed boundary and compare the decay trace with the baseline taken at the start. The same elapsed-time checkpoints must be used again so the comparison is credible. |
| T7 | Reinspect the feed fuse, connector, wake line support, and ground return after the repeated decay test because a branch can return to high draw only after the loom and covers are back in their final positions. This post-test reread ties the result to the installed state. |
| T8 | Finish the sleep-current investigation only when the report names the current-retaining circuit, the exact hardware that prevented decay, the before and after decay trace, and the final branch condition after reassembly. The release boundary is a pack whose sleep current falls through the required time window with no renewed retained wake or feed path. |
