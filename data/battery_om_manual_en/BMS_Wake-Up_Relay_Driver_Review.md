| Time step | O&M sample text |
|---|---|
| T1 | On Kestrel BMSGate-447 LR, record whether the pack fails to wake from charger insertion, key request, or service-tool command, and note which wake prerequisites were present before the relay-driver path is inspected. Capture the exact event that should energize the wake relay. |
| T2 | Expose the BMS wake-driver path, including the low-voltage connector, driver output pin, relay coil feed, coil return, relay socket, and flyback diode or suppression component if fitted, while preserving the original harness lay. The command source and the coil load need to stay in the same evidence chain. |
| T3 | Confirm that the wake request reaches the controller, then measure whether the driver output changes state and whether the relay coil receives the expected voltage under load, recording the timing from request to driver transition and from driver transition to relay pull-in if the platform data allows it. A valid request with no driver output belongs to a different boundary than a driver output that is present but lost across the relay socket or delayed enough to miss the BMS wake window. |
| T4 | Inspect the relay socket terminals, coil pins, driver pin retention, suppression component, and local ground reference for looseness or heat witness, then compare the coil response with the measured driver output. This tells whether the wake path fails in control, in load, or in the socket interface between them. |
| T5 | Correct only the failed driver output, relay coil, socket terminal, suppression component, or feed path, then repeat the same wake request with the branch restored to its final routed condition. The repeated test must prove that command, coil voltage, and relay action now line up through the full event and remain stable when the connector shell, socket body, and first loom support are back under their normal installed preload. |
| T6 | Finish the wake-circuit review only when the note names the exact failed item and records the command, output, and relay response that confirmed recovery, including any measured timing if that timing was part of the original complaint. The release boundary is a wake path that energizes the relay on demand, holds the correct voltage at the coil, and remains stable after the connector and loom supports are returned to place with no renewed delay or drop-out. |
