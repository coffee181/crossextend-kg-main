| Time step | O&M sample text |
|---|---|
| T1 | For Helion TempPack-553, record which thermal sensors are being compared and under what operating condition, such as soak, charge, or coolant circulation, before any judgment is made about channel mismatch. Save the physical locations and the compared values at the same moment. |
| T2 | Pull the live data for the full sensor group and arrange it by physical location so electrical outliers can be separated from real thermal gradients across the pack. The comparison must follow the actual module and coolant topology, not sensor list order alone. |
| T3 | Compare neighboring sensors that share the same module face, coolant plate, or thermal zone, then note which channel diverges in absolute value, response rate, or both. A slow sensor and a biased sensor point to different boundaries. |
| T4 | Inspect the most divergent channels for loose thermal pads, lost clips, shifted sensor bodies, connector drag, or damaged lead support before blaming controller logic. Record whether the hardware evidence sits at the sensor seat, the connector, or the support path. |
| T5 | Recheck the correlation after any physical correction or after a stable thermal hold, making sure the channels now follow each other in both absolute value and rate of change. The repeated hold has to use the same physical comparison group named at the start. |
| T6 | Compare the thermal-sensor group against a known coolant or module reference so real thermal imbalance is separated from one bad mounting or one bad branch-resistance path. This keeps the diagnosis on a named hardware boundary instead of on a generic sensor mismatch. |
| T7 | Reinspect the corrected sensor seat, clip, and lead support after the hold because a sensor that tracks correctly while touched can drift again when the branch relaxes back into its natural position. This post-hold reread ties the result to the final support geometry. |
| T8 | Repeat the thermal comparison after nearby clips and covers are restored so the accepted state reflects the final loaded route of the sensor branch and not only an open-bay setup. The correlation has to survive the real packaged condition. |
| T9 | Close the sensor-correlation review only when the note identifies whether the mismatch came from real thermal imbalance, sensor mounting error, branch resistance, or controller interpretation, and names the compared sensor locations plus the final repeated result. The release boundary is a sensor group that tracks within the expected spread and response window under the same operating condition that originally exposed the mismatch. |
