| Time step | O&M sample text |
|---|---|
| T1 | For Arcline BMSNet-574, record the communication-drop conditions, including whether the fault appears during charge, wake, vibration, or thermal soak, and note which modules or controllers disappear from the network together. Save the first affected network state before opening the loom. |
| T2 | Expose the BMS CAN branch, twisted pair, splice pack, termination path, shield drain, and connector set around the controller housing while preserving the original twist pitch and clip positions. Keep the suspect splice and one healthy neighboring branch visible together for comparison. |
| T3 | Measure network resistance with power down, then check CAN high, CAN low, and shield drain continuity section by section so the fault boundary is reduced before the loom is disturbed. Record the values at splice, connector, and termination points rather than only as a total, and note whether the branch still holds the expected near-60 ohm backbone value or drifts only when one named segment is connected. |
| T4 | Backprobe the branch during live communication and gently load the twisted pair at the connector rear, splice pack, and first support clip while watching for waveform collapse, recessive-level shift, or sudden loss of differential amplitude that identifies the intermittent span. A change that appears only when the first clip is moved belongs to the routed harness or shield-drain support path, not to the controller itself. |
| T5 | Inspect splice integrity, terminal drag, shield-drain landing, and clip pressure if the branch looks correct at rest, because intermittent CAN faults often come from retention loss rather than visible conductor damage. Compare the suspect readings with a neighboring healthy branch measured in the same operating state. |
| T6 | Correct only the failed CAN high span, CAN low span, splice cavity, shield drain eyelet, or connector terminal, then repeat the live and power-down network checks with the loom returned to its final twist and clip geometry. The corrected branch has to survive the same wake or charge state that originally produced the dropout. |
| T7 | Close the network trace only when the report names the exact failing segment, the measured resistance or waveform change, and the final harness condition from splice to connector. The release boundary is a CAN branch that holds stable communication under the original operating state with no renewed change when the restored clips and covers preload the loom. |
