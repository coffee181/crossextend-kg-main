| Time step | O&M sample text |
|---|---|
| T1 | On Velorian CurrentPack-604, record the claimed current offset at rest and under load, and note whether the deviation affects both charge and discharge or only one direction, before the Hall sensor assembly is disturbed. Save the reference current source and the exact operating state used for comparison. |
| T2 | Expose the Hall sensor body, conductor pass-through, supply connector, reference ground, signal line, mounting seat, and the first support clip while preserving the as-found sensor alignment. Keep the sensor core and its connector visible together because zero offset can originate in either the sensing element or the support path. |
| T3 | Verify supply voltage, ground quality, and signal baseline with no current flowing, then compare the sensor output with a trusted shunt or reference channel under a known low load and again under a higher load. Record the offset at both load points so zero shift and gain error are not blended together. |
| T4 | Inspect the mounting seat and conductor pass-through for tilt, trapped debris, metallic particles, or burrs that could shift the core relative to the conductor. A skewed sensor body changes the magnetic path differently than a pure supply or terminal fault. |
| T5 | If the signal drifts with harness movement, check connector drag, terminal retention, and the first bend behind the connector before blaming the Hall element itself. Movement sensitivity belongs to the connector branch unless the core and seat remain mechanically stable. |
| T6 | Compare the sensor output slope against the reference channel through both current directions so the diagnosis lands on Hall core offset, gain error, mounting-seat contamination, supply-ground issue, or signal-terminal retention with evidence for each. This is where the electrical and geometric readings must agree. |
| T7 | Correct only the failed sensor body, mounting seat, connector terminal, supply-ground path, or support geometry, then repeat the same no-load and loaded measurements with the sensor returned to its final bracket and clip positions. The repeated proof must use the same current states recorded at the start. |
| T8 | Reinspect the mounting seat, connector lock, and support clip after the second load sequence because a sensor that passes one offset check can drift again once the harness settles or the bracket warms. This final reread keeps the result tied to the installed state. |
| T9 | Recreate the reference measurement once more after all covers and brackets are restored so the accepted result represents the full assembled path from sensor core to connector and support clip. The branch is not cleared if the offset returns only after final mounting preload is back in place. |
| T10 | Finish the current-sensor diagnosis only when the report names the exact source, such as Hall sensor body, mounting-seat contamination, supply-ground issue, or signal-terminal retention, and records the no-load and loaded offsets before and after correction. The release boundary is a sensor path that tracks the reference channel through both current directions with stable seating and no movement-sensitive drift. |
