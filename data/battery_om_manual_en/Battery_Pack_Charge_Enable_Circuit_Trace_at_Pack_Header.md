| Time step | O&M sample text |
|---|---|
| T1 | For Helion ChargePack-621, record the no-charge or no-enable complaint and note whether the pack sees charger presence, wake request, and BMS permission before the pack-header trace begins. Save the exact charge-request state that should assert the enable path. |
| T2 | Expose the charge-enable circuit at the pack header, including the enable pin, return path, connector cavities, back shell, and the first harness span between the header and the controller or charger interface while preserving the original harness lay. Keep the full enable path visible from output to first support. |
| T3 | Measure voltage or continuity at each segment of the enable path during a live charge request so the missing signal is localized to header pin, harness, or controller output rather than diagnosed from scan data alone. Record the measurement point and the charge state at each reading. |
| T4 | Inspect connector drag, rear-seal condition, and wire support at the first bend behind the header because charge-enable circuits often fail where the small-gauge wire is repeatedly stressed. A weak header cavity and a broken first-bend conductor create different recovery paths. |
| T5 | Compare the suspect pin position and signal behavior with a neighboring logic circuit at the same connector so the diagnosis rests on observed electrical difference rather than on connector appearance alone. This keeps the trace on one cavity and one branch splice. |
| T6 | Correct only the failed header pin, harness span, controller output, return path, or branch splice, then repeat the same live charge request with the back shell and supports restored. The enable branch has to prove itself in the final packaged geometry. |
| T7 | Reinspect the header cavity, back shell, and first support after the repeated request because a repaired small-gauge branch can reopen only after the loom settles into its closed position. This post-test reread anchors the fix to the real support path. |
| T8 | Repeat the key measurement once more under the same charge-request state after all covers are back in place so the accepted result includes the final preload on the header branch. The branch is not released if the signal disappears only after the back shell is loaded. |
| T9 | Close the charge-enable trace only when the report names the exact failing point, the charge-request state used, the measured values before and after repair, and the final seated condition at the header path. The release boundary is an enable branch that carries the expected signal through the named cavity and harness span with no renewed drop after reassembly. |
