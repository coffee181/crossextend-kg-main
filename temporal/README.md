# Temporal Support

This directory keeps the optional temporal and lifecycle utilities used by the
current graph assembly layer.

## Current Role

- temporal assertions from graph assembly
- optional lifecycle event detection
- temporal consistency validation helpers

These modules remain in the repository because the active pipeline still imports
them when snapshot or lifecycle support is enabled.
